Propose a planning stage plan for the following goal. Use the stage catalogue, catalogue version, and current revisions from the system prompt.

{{ goal }}
{{ if previous_failure }}

The preceding plan was rejected for this reason. Fix only that defect while preserving the intent:
{{ previous_failure }}
{{ end }}

Return exactly one JSON object and nothing else.
