Propose the next planning loop decision for the goal below. Use the design, workflow state, fresh revisions, and budget from the system prompt.

{{ goal }}
{{ if previous_failure }}

The preceding decision was rejected for this reason. Fix only that defect while preserving the intent:
{{ previous_failure }}
{{ end }}

Return exactly one JSON object and nothing else.
